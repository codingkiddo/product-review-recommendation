server:
  port: 8080
  error:
    include-message: always

spring:
  config:
    activate:
      on-profile:
      - docker
#  application:
#    name: product-service



logging:
   level:
      root: INFO
      se:
        magnus:
          microservices: DEBUG