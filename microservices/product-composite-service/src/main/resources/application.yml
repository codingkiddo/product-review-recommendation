server:
  port: 8000
  error:
    include-message: always

spring:
  config:
    activate:
      on-profile:
      - docker

app:
  product-service:
    host: localhost
    port: 8001
  recommendation-service:
    host: localhost
    port: 8002
  review-service:
    host: localhost
    port: 8003

logging:
   level:
      root: INFO
      se:
        magnus:
          microservices: DEBUG